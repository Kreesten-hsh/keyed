{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Quickstart","text":"<p><code>keyed</code> is a lightweight local FastAPI authentication library for issuing, verifying, and rate-limiting API keys. All validation happens in-process and against your own PostgreSQL database in sub-5ms with zero external cloud proxies.</p>"},{"location":"#5-minute-example","title":"5-Minute Example","text":"<pre><code>import os\nfrom uuid import UUID\n\nfrom fastapi import Depends, FastAPI\nfrom keyed import Keyed\nfrom keyed.core.models import AuthenticatedAPIKey\n\napp = FastAPI(title=\"My Protected API\")\n\n# Initialize keyed with your PostgreSQL database URL\nkeyed = Keyed(os.environ[\"KEYED_DATABASE_URL\"])\n\n\n# Protect any route with scopes and automatic rate-limiting\n@app.get(\"/v1/documents\")\nasync def list_documents(\n    principal: AuthenticatedAPIKey = Depends(\n        keyed.auth.require_scopes(\"documents:read\")\n    ),\n) -&gt; dict[str, str]:\n    return {\"tenant_id\": str(principal.tenant_id), \"status\": \"authorized\"}\n\n\n# Issue an API key for a client (run via script, seed, or onboarding endpoint)\nasync def create_client_key(tenant_id: UUID) -&gt; str:\n    issued = await keyed.issue_key(\n        tenant_id=tenant_id,\n        scopes=[\"documents:read\"],\n        rate_limit_per_minute=60,\n        environment=\"live\",\n    )\n    # The plaintext is returned only once at creation\n    return issued.plaintext\n</code></pre>"},{"location":"#what-happens-on-each-request","title":"What Happens on Each Request","text":"<ol> <li>Header Extraction: <code>keyed</code> reads the incoming <code>X-API-Key</code> header.</li> <li>In-Memory Rate Limiting: Checks sliding-window counters in memory (&lt;1ms). Returns <code>429 Too Many Requests</code> with a <code>Retry-After</code> header if exceeded.</li> <li>Constant-Time Verification: Looks up the key by its dynamic prefix, retrieves the salted SHA-256 hash from PostgreSQL, and compares using <code>hmac.compare_digest</code>.</li> <li>Scope &amp; Revocation Check: Ensures the key is not expired, not revoked, and holds the required scopes before executing your route logic.</li> </ol>"},{"location":"api-reference/","title":"API Reference","text":"<p>The <code>Keyed</code> class is the primary integration point for the library.</p> <pre><code>from keyed import Keyed\n\nkeyed = Keyed(database_url=\"postgresql+asyncpg://user:pass@host:5432/db\")\n</code></pre>"},{"location":"api-reference/#1-authentication-dependency","title":"1. Authentication Dependency","text":""},{"location":"api-reference/#keyedauthrequire_scopesscopes-str","title":"<code>keyed.auth.require_scopes(*scopes: str)</code>","text":"<p>Creates a FastAPI dependency that extracts <code>X-API-Key</code>, enforces sliding-window rate limits, verifies the hash in constant time, and asserts that the key possesses all specified scopes.</p> <pre><code>@app.get(\"/analytics\")\nasync def get_analytics(\n    principal: AuthenticatedAPIKey = Depends(\n        keyed.auth.require_scopes(\"analytics:read\", \"reports:read\")\n    ),\n):\n    return {\"tenant_id\": str(principal.tenant_id), \"scopes\": principal.scopes}\n</code></pre> <ul> <li>Headers: <code>X-API-Key: &lt;plaintext_key&gt;</code></li> <li>Success: Injects <code>AuthenticatedAPIKey</code> (contains <code>id</code>, <code>tenant_id</code>, <code>name</code>, <code>scopes</code>, <code>environment</code>).</li> <li>Errors:</li> <li><code>401 Unauthorized</code>: Missing, expired, revoked, or invalid key format/hash.</li> <li><code>403 Forbidden</code>: Key is valid but lacks one of the required scopes.</li> <li><code>429 Too Many Requests</code>: Exceeded <code>rate_limit_per_minute</code> (includes <code>Retry-After: &lt;seconds&gt;</code>).</li> </ul>"},{"location":"api-reference/#2-key-management","title":"2. Key Management","text":""},{"location":"api-reference/#keyedissue_key","title":"<code>keyed.issue_key(...)</code>","text":"<p>Generates and persists a new cryptographically secure API key.</p> <pre><code>issued = await keyed.issue_key(\n    tenant_id=UUID(\"...\"),\n    name=\"Production Agent Key\",\n    scopes=[\"documents:read\", \"documents:write\"],\n    rate_limit_per_minute=120,\n    expires_at=None,\n    environment=\"live\", # or \"test\"\n)\n\n# Access plaintext (returned only here, never stored)\nprint(issued.plaintext) # e.g. key_live_abc123...\nprint(issued.id)        # UUID primary key\nprint(issued.prefix)    # e.g. key_live_abc1\n</code></pre>"},{"location":"api-reference/#keyedrevoke_keykey_id-uuid-tenant_id-uuid","title":"<code>keyed.revoke_key(key_id: UUID, tenant_id: UUID)</code>","text":"<p>Instantly revokes an API key in PostgreSQL. Future requests using this key will immediately return <code>401 Unauthorized</code>.</p> <pre><code>await keyed.revoke_key(key_id=key_uuid, tenant_id=tenant_uuid)\n</code></pre>"},{"location":"api-reference/#3-rate-limiting-behavior","title":"3. Rate Limiting Behavior","text":"<p>The in-process sliding window monitors request timestamps per key in memory: * Latency: <code>&lt; 1ms</code> check overhead. * Scope: In-process counters (single-worker per process).  * Header: Returns <code>Retry-After</code> indicating the remaining wait time in seconds before the window resets.</p>"},{"location":"installation/","title":"Installation &amp; Setup","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python <code>&gt;= 3.11</code></li> <li>PostgreSQL <code>&gt;= 14</code></li> <li>FastAPI <code>&gt;= 0.115</code></li> </ul>"},{"location":"installation/#1-package-installation","title":"1. Package Installation","text":"<p>Install <code>keyed-api-auth</code> in your Python environment:</p> <pre><code>pip install keyed-api-auth\n</code></pre> <p>Or when developing locally:</p> <pre><code>python -m venv .venv\nsource .venv/bin/activate\npip install -e '.[dev]'\n</code></pre>"},{"location":"installation/#2-environment-configuration","title":"2. Environment Configuration","text":"<p>Define your PostgreSQL connection string in your environment (or <code>.env</code> file):</p> <pre><code>export KEYED_DATABASE_URL=\"postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/keyed\"\n</code></pre>"},{"location":"installation/#3-database-migrations","title":"3. Database Migrations","text":"<p>Apply the Alembic schema migrations to create the <code>api_keys</code> table:</p> <pre><code>alembic upgrade head\n</code></pre>"},{"location":"installation/#4-run-with-docker-compose-optional","title":"4. Run with Docker Compose (Optional)","text":"<p>If you prefer running a local isolated stack with PostgreSQL and FastAPI ready to go:</p> <pre><code>cp .env.example .env\ndocker compose up --build\n</code></pre> <p>The service will start on <code>http://127.0.0.1:58000</code>: * <code>GET /health</code> : Public health check (<code>200 OK</code>) * <code>GET /v1/whoami</code> : Sample protected route (<code>401 Unauthorized</code> without a valid key)</p>"}]}